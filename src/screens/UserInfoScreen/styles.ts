import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 24,
    },
    spaceForm: {
        marginBottom: 16,
    },
    spaceButton: {
        marginTop: 20,
    },
    spaceButton2: {
        marginTop: 30,
    },
    spaceHeader: {
        marginBottom: 20,
    },
});

export default styles;
