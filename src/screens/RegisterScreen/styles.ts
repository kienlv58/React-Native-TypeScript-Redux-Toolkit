import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 24,
    },
    spaceForm: {
        marginBottom: 16,
    },
    spaceButton: {
        marginTop: 30,
    },
    spaceHeader: {
        marginBottom: 54,
        marginTop: 80,
    },
});

export default styles;
